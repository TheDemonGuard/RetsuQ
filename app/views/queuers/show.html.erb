<% if current_user.id == @restaurant.user_id %>
  <% @queuers.each do |queue| %>
    <%= queue.user_id  %>
  <% end %>
<% else %>
  <h1>You made it</h1>
  <%= link_to "Remove", queuer_path(@queuer), method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>
